# КіноАфіша

**Виконавець:** Павлова Єлизавета, П-31

---

## Лабораторна робота №1 — Семантична розмітка та мультимедіа

**Тема:** Семантична розмітка, мультимедіа та проектування інтерфейсів
**Мета:** Створити логічний архітектурний каркас веб-застосунку та інтерфейс збору даних за допомогою HTML5.

### Використані технології
- **HTML5 Semantic Markup**: `<header>`, `<nav>`, `<main>`, `<footer>`, `<article>`, `<section>`, `<figure>`.
- **Multimedia Optimization**: `<picture>` (webp/jpg), `<video>` з атрибутами розміру, `loading="lazy"`.
- **Performance**: Оптимізація LCP через `preload` та `fetchpriority`, стабілізація CLS через атрибути `width`/`height`.

---

## Лабораторна робота №2 — CSS стилізація

**Тема:** Типографіка, кольорова палітра, Box Model, інтерактивні елементи та навігація
**Мета:** Створити єдину систему стилів для трьох сторінок проєкту через зовнішній файл `css/style.css`.

### Завдання 1 — Типографіка та колір
- CSS-змінні для палітри: `--ink` (#1a0f0a), `--copper` (#c4956a), `--parchment` (#e8d5b7), `--cream` (#f2e8d5), `--copper-dim` (#8a6347), `--ink-soft` (#2d1f17).
- Шрифти через Google Fonts: **Playfair Display** (заголовки h1-h3) та **Lora** (body, форми).
- Одиниці виміру: `rem` для розмірів шрифту, `em` для відступів.
- Ієрархія заголовків: h1 — 2.8rem/900, h2 — 2rem/700, h3 — 1.4rem/600 з різним `letter-spacing`.

### Завдання 2 — Box Model та Layout
- Глобальний `box-sizing: border-box`.
- Клас `.container` з `max-width: 1200px`, центруванням та боковими відступами.
- Картки фільмів з `border-radius: 4px`, `overflow: hidden` та теплими тінями `box-shadow`.

### Завдання 3 — Форма та Таблиця
- Поля введення: тільки нижня рамка `border-bottom`, прозорий фон, плейсхолдери курсивом через `::placeholder`.
- Плавні переходи `transition` для фокусу на полях.
- Кнопка submit зі зміною кольору та `transform: translateY(-2px)` при наведенні.
- Таблиця розкладу: `border-collapse`, ефект зебри через `:nth-child(even)`, заголовки `<th>` з `uppercase` та розрядкою літер.

### Завдання 4 — Навігація
- Список меню `ul` з обнуленими стилями, `li` через `display: inline-block`.
- Усунення пробілів між елементами через `font-size: 0` на батьківському елементі.
- Посилання `<a>` з нижньою лінією при hover та активному стані `.active`.

### Структура файлів
```
css/
  style.css       /* головна таблиця стилів для всіх сторінок */
  style3.css      /* стилі для просунутого рівня */
js/
  script.js       /* джаваскрипт логіка масивів мап та сет */
index.html        /* головна сторінка з прем'єрою та скоро в кіно */
catalog.html      /* каталог фільмів за жанрами та розклад сеансів */
contacts.html     /* форма зворотного зв'язку */
```

---

## Практична робота 1 — просунутий рівень

**Тема:** адаптивний дизайн мобайл ферст флексбокс грід та бургер меню
**Мета:** переробити інтерфейс на сучасні css підходи з адаптивністю

### завдання 1 — ініціалізація та мобайл ферст
- базова типографіка та кольори через css змінні
- мінімальна ширина 320px для мобільних

### завдання 2 — навігація на флексбокс
- перехід з inline block на flex
- justify content та gap для відступів

### завдання 3 — макроструктура на грід
- грід лейаут для всього body
- зони header main sidebar footer

### завдання 4 — сітка каталогу через автофіт
- repeat auto fit minmax для жанрових секцій
- гнучка кількість колонок залежно від ширини

### завдання 5 — адаптивне меню бургер
- чекбокс хак для перемикача без джаваскрипта
- анімація зміни іконки на хрестик

### завдання 6 — медіа запити для планшетів та десктопів
- сучасний синтаксис width більше або дорівнює 768px
- планшет md та десктоп lg брейкпоінти

### завдання 7 — темна тема та фіналізація футера
- prefers color scheme dark
- флексбокс для вирівнювання футера

**тег:** пр1_просунутий_рівень

---

## Практична робота 2 — просунутий рівень

**Тема:** інтеграція tailwind css та переробка інтерфейсу на утилітарні класи
**Мета:** відмовитися від власних css правил на користь фреймворку

### завдання 1 — підключення tailwind
- cdn підключення з кастомною конфігурацією кольорів
- брендові кольори ink copper parchment cream

### завдання 2 — макроструктура через утиліти
- min h screen flex flex col bg cream text ink для body
- max w 7xl mx auto px 4 для контейнера
- mt auto для прибиття футера

### завдання 3 — картки фільмів з hover ефектами
- bg white rounded lg shadow lg
- hover scale 105 transition duration 300

### завдання 4 — адаптивна навігація
- hidden md flex для приховування меню на мобільних
- block md hidden для бургера

### завдання 5 — стилізація форм та таблиць
- focus ring 2 focus border copper для інпутів
- overflow x auto min w 600px для гумової таблиці

### завдання 6 — адаптивні колонки
- grid cols 1 sm grid cols 2 lg grid cols 3
- 1 колонка мобайл 2 планшет 3 десктоп

**тег:** пр2_просунутий_рівень

---

## Лабораторна робота 3 — просунутий рівень

**Тема:** робота з масивами обєктів колекціями та доступ до полів
**Мета:** обробити дані про фільми через джаваскрипт методи

### завдання 1 — фільтрація та трансформація масиву
- метод filter для відбору фільмів з рейтингом вище 7
- метод map для створення масиву назв фільмів

### завдання 2 — унікальні формати через set
- new set з масиву булевих значень is 3d
- отримання унікальних форматів перегляду

### завдання 3 — словник цін через map
- new map для пар назва фільму ціна
- метод foreach для заповнення
- метод get для швидкого пошуку

### завдання 4 — доступ до полів з пробілами
- квадратні дужки landmark["landmark name"]
- вкладені обєкти geo з lat та lng

**тег:** лр3_просунутий_рівень

---

## Як запустити для Lighthouse
Для отримання 100% за Accessibility та Performance (кешування), запустіть:
```bash
python server.py
```
Сайт буде доступний на [http://localhost:8002/](http://localhost:8002/) з правильними HTTP-заголовками кешування.
